name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Backend
        run: curl https://agendamiento-citas-gyw9.onrender.com/health

      - name: Ping Keycloak
        run: curl https://keycloak-clinica.onrender.com